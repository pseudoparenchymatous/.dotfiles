# vim: set filetype=tmux :
set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.local/share/tmux/plugins'
set -g @plugin 'tmux-plugins/tpm'

# set -g @plugin 'egel/tmux-gruvbox'
# set -g @tmux-gruvbox 'dark' # or 'light'

set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @resurrect-dir '~/.local/share/tmux/plugins/resurrect'
set -g @resurrect-strategy-nvim 'session'

set -g @plugin 'tmux-plugins/tmux-battery'
set -g @batt_icon_status_discharging '♥️ '

set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @plugin 'erikw/tmux-powerline'
# set -g status-justify "centre"
# set -g status-left-length 60
# set -g status-right-length 90
# set -g status-left "#(~/.local/share/tmux/plugins/tmux-powerline/powerline.sh left)"
# set -g status-right "#(~/.local/share/tmux/plugins/tmux-powerline/powerline.sh right)"
# set-hook -g session-created 'run-shell "~/.local/share/tmux/plugins/tmux-powerline/powerline.sh init"'

set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @prefix_highlight_prefix_prompt '😳'
set -g @prefix_highlight_bg 'colour237' 

run '~/.local/share/tmux/plugins/tpm/tpm'

